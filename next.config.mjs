/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
  images: {
    domains: ['localhost'], // Ajoutez d'autres domaines si nécessaire
  },
};

export default nextConfig;

